{
  "name": "queue",
  "version": "1.0.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"mkdir dist; cp -r src/* dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.8.0",
    "csv-parser": "^3.0.0",
    "dotenv": "^10.0.0",
    "mongodb": "^4.2.1",
    "winston": "^3.3.3"
  }
}
